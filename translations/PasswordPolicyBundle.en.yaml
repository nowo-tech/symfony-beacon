# Beacon overrides for nowo-tech/password-policy-bundle (toast title + body).
nowo_password_policy:
    title: Your password has expired
    message: Update it under Account → Security to keep signing in.
    password_in_history: Cannot reuse a recent password. You used this one {{ days }} days ago.
