# Implementation Plan: Performance

**Status**: Completed historically (through v0.6.0).

As-built: `Transaction`/`Span` persistence, `NPlusOneDetector` (MIN_REPEATS=5), UI `/projects/{id}/performance` with `?nplus1=1`.

See `spec.md`. No further plan work unless a new Performance epic is specified.
