{# Host override: Beacon shell already loads kit styles before app CSS.
   Do NOT append nowo-ui.css again — that reasserts UiKit blue/slate :root tokens. #}
{% extends layout|default(nowo_routing_kit_layout_template) %}
{% import '@NowoUiKitBundle/macros/ui.html.twig' as ui %}
{% set _css_fw = nowo_routing_kit_css_framework|default('tailwind') %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}
