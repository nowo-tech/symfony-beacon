twig:
    file_name_pattern: '*.twig'
    form_themes:
        - '@NowoFormKit/form/static_blocks.html.twig'
        - '@NowoPhoneInputBundle/Form/phone_input_widget.html.twig'
        - 'form/beacon_theme.html.twig'
        # Kit requirement/generator copy stays on NowoPasswordStrengthBundle even when
        # FormKit sets field translation_domain to form (AccountSecurity / AuthKit).
        - 'form/password_strength_beacon_theme.html.twig'
    globals:
        enabled_locales: '%kernel.enabled_locales%'
        default_locale: '%kernel.default_locale%'
        session_cookie_name: '%beacon.session_cookie_name%'
        site_appearance: '@App\Shared\Appearance\SiteAppearanceProvider'
        app_section: '@App\Shared\Navigation\AppSectionResolver'


when@dev:
    twig:
        # Better Web Profiler Twig panel + Twig Inspector comments
        debug: true

when@test:
    twig:
        strict_variables: true
