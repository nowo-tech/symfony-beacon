framework:
    rate_limiter:
        magic_login:
            policy: fixed_window
            limit: 5
            interval: '15 minutes'

when@test:
    framework:
        rate_limiter:
            magic_login:
                policy: fixed_window
                limit: 1000
                interval: '1 minute'
