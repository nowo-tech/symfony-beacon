# Prometheus scrape hardening.
# Production requires a non-empty BEACON_METRICS_TOKEN (503 until configured).
# Scrapers use Authorization: Bearer only; ROLE_ADMIN session still works when a token is set.
when@prod:
    parameters:
        env(BEACON_METRICS_REQUIRE_TOKEN): '1'
