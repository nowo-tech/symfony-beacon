# Prometheus scrape hardening notes.
# Metrics token + require-token live under Administration → Ops defaults (instance_settings).
# Scrapers use Authorization: Bearer only; ROLE_ADMIN session still works when authorized.
# Production: enable "Require metrics token" and set a non-empty token in Ops defaults.
