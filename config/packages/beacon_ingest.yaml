# Envelope ingest auth hardening.
# Default (all envs): reject deprecated query beacon_key/beacon_secret (see parameters.yaml).
# Set BEACON_INGEST_REJECT_QUERY_AUTH=0 only while migrating clients to X-Beacon-Auth or envelope dsn.
# Tests that exercise the legacy path set the env to 0 for that case only.
