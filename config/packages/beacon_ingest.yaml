# Envelope ingest auth hardening.
# Reject deprecated query beacon_key/beacon_secret under Administration → Ops defaults (default: on).
# Disable only while migrating clients to X-Beacon-Auth or envelope dsn.
