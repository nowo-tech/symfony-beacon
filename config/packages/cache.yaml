framework:
    cache:
        pools:
            cache.rate_limiter:
                adapter: cache.app

when@test:
    framework:
        cache:
            app: cache.adapter.array
            pools:
                cache.rate_limiter:
                    adapter: cache.adapter.array
