# Notifications SSRF / private URL policy.
# Allow-private-URLs is configured under Administration → Ops defaults (default: blocked).
# Enable only for local webhooks to private hosts (host.docker.internal, etc.).
